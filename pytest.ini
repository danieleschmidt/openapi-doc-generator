[pytest]
testpaths = tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    benchmark: marks tests as performance benchmarks
# Use parallel execution: pytest -n auto (faster for development)
# Use with coverage: pytest --cov=src/openapi_doc_generator --cov-report=term
